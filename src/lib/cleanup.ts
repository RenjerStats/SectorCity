/** Подписи и объяснения причин очистки (`CleanupReason`) и уровней уверенности
 *  (`Confidence`) для панели причин, карточки и тултипа кандидата. */
import type { CleanupReason, Confidence } from "./ipc/contract";

/** Подпись + объяснение словами «почему это мусор» для каждой причины. */
export const REASON_META: Record<
  CleanupReason,
  { label: string; explain: string }
> = {
  packageCache: {
    label: "Кэш пакетов",
    explain:
      "Кэш пакетного менеджера (node_modules, pip, NuGet, Gradle). Пересоздаётся автоматически при следующей установке зависимостей.",
  },
  buildArtifact: {
    label: "Артефакты сборки",
    explain:
      "Результаты сборки проекта (target, build, dist). Пересобираются из исходников — удаление стоит лишь времени следующей сборки.",
  },
  browserCache: {
    label: "Кэш браузера",
    explain:
      "Кэш страниц и медиа браузера. Безопасно удалять: браузер накачает заново, первые открытия сайтов будут чуть медленнее.",
  },
  gpuCache: {
    label: "Кэш GPU/шейдеров",
    explain:
      "Скомпилированные шейдеры драйвера и приложений (DXCache, GLCache). Пересоздаются на лету; после чистки возможны короткие фризы в играх.",
  },
  tempDir: {
    label: "Папка времянок",
    explain:
      "Системная папка временных файлов (Temp). Программы обязаны переживать её очистку; занятые файлы система удалить не даст.",
  },
  tempFile: {
    label: "Файлы-времянки",
    explain:
      "Временные и резервные файлы по расширению (.tmp, .bak, .old, ~*). Обычно забытые черновики и копии.",
  },
  interruptedDownload: {
    label: "Прерванные загрузки",
    explain:
      "Недокачанные файлы (.crdownload, .part). Загрузка прервана — остаток бесполезен, докачка начнёт заново.",
  },
  crashDump: {
    label: "Дампы падений",
    explain:
      "Слепки памяти упавших программ (.dmp, CrashDumps). Нужны только для разбора конкретного сбоя.",
  },
  recycleBin: {
    label: "Корзина",
    explain:
      "Содержимое Корзины — уже удалённые файлы. Снос освобождает место окончательно, восстановить их будет нельзя.",
  },
  windowsOld: {
    label: "Windows.old",
    explain:
      "Прошлая установка Windows. Нужна только для отката обновления; удаление делает откат невозможным.",
  },
  installerInDownloads: {
    label: "Установщики в Загрузках",
    explain:
      "Скачанные установщики (.exe, .msi) в папке «Загрузки». После установки обычно не нужны, но проверьте, не храните ли их сознательно.",
  },
  emptyDir: {
    label: "Пустые папки",
    explain:
      "Папки без содержимого. Места не занимают, но захламляют структуру.",
  },
  staleLarge: {
    label: "Крупное и старое",
    explain:
      "Файл больше 100 МБ, не тронутый больше полугода. Это лишь эвристика — решайте по содержимому.",
  },
};

/** Метаданные уверенности: подпись, пояснение и CSS-переменная цвета глифа. */
export const CONFIDENCE_META: Record<
  Confidence,
  { label: string; hint: string; cssVar: string }
> = {
  safe: {
    label: "безопасно",
    hint: "Пересоздаётся само — можно сносить смело",
    cssVar: "var(--safe)",
  },
  likely: {
    label: "скорее мусор",
    hint: "Почти наверняка мусор, но взгляните на список",
    cssVar: "var(--stale)",
  },
  review: {
    label: "на ваш выбор",
    hint: "Только по решению пользователя",
    cssVar: "var(--text-muted)",
  },
};
