{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Базовые разрешения главного окна: ядро + диалог выбора папки + открытие в проводнике.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
